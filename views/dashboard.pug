extends layout

block content

  nav.navbar
    .navbar-brand Dashboard
    .navbar-menu
      a.nav-item(href="/clock-in") 
        span.icon ⏰
        | Clock In
      a.nav-item(href="/profile") 
        span.icon 👤
        | Profile
      a.nav-item(href="/reports") 
        span.icon 📊
        | Reports
      a.nav-item(href="/work-templates") 
        span.icon 📋
        | Work Templates
      a.nav-item(href="/logout") 
        span.icon 🚪
        | Logout
  
  .container
    .welcome-section
      h2 Welcome, #{user.userFirstname} #{user.userLastName}!
      h4 User lvl: #{user.userAdminLvl}
      p.current-time Current Time: 
        span#clock
    
    .dashboard-grid
      .card
        h3 ⏰ Clock In/Out
        p Quick access to clock in or out
        a.btn(href="/clock-in") Clock In Now
      
      .card
        h3 👤 Your Profile
        p View and edit your profile information
        a.btn(href="/profile") View Profile
      
      .card
        h3 📊 Reports
        p View your work reports and statistics
        a.btn(href="/reports") View Reports
      
      .card
        h3 📋 Work Templates
        if user.userAdminLvl < 10
          p Administrate work templates
          a.btn(href="/work-templates") Manage Templates
        else
          p View available work templates
          a.btn(href="/work-templates") View Templates

  style.
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      background: #f5f5f5;
    }
    
    .navbar {
      display: flex;
      align-items: center;
      justify-content: space-between;
      background-color: #e0e0e0;
      padding: 10px 20px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }

    .navbar-brand {
      font-weight: bold;
      font-size: 1.3rem;
      color: #333;
    }

    .navbar-menu {
      display: flex;
      gap: 15px;
    }

    .nav-item {
      text-decoration: none;
      color: #333;
      font-weight: 500;
    }

    .nav-item:hover {
      text-decoration: underline;
    }
